# Server and Database Setup TODO

## Plan Steps:

1. ✅ Create .env file with MONGODB_URI placeholder.
2. ✅ Create app.js (Express app, middleware, DB connect).
3. ✅ Create .env file with MONGODB_URI placeholder.
4. ✅ Create app.js (Express app, middleware, DB connect).
5. ✅ Edit server.js (import app, start server).
6. ✅ Setup complete.

**Next:**

- Update .env with your real MongoDB URI (create free Atlas cluster if needed).
- Run `npm run dev` to start server with nodemon.
- Test: http://localhost:5000 should show JSON response.
- DB connects automatically on start.
